app:
  configuration:
    name: test
    version: test
    description: test
    contact:
      name: test
      email: test
      role: test
    cache:
      type: map
      clear-cron: "0 * * * * *"
  currency:
    synchronization-cron: "0 * * * * *"
    reschedule-when-synchronization-failed-cron: "0 * * * * *"
    clean-reschedule-cron: "0 * * * * *"
    provider:
      nbp:
        name: nbp
        client: npb-client
        url: test
        details:
          data-format: json
          currency: PLN
          history-from-year: 2002
          max-days-to-fetch: 90

spring:
  profiles:
    active: test